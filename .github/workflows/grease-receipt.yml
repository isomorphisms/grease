name: grease-receipt

on:
  pull_request:
  push:
    branches: [main]

jobs:
  cpp-spec:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out Grease and pinned Oils source
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Verify pinned source and Ubuntu host
        shell: bash
        run: |
          revision=$(git rev-parse HEAD:source)
          test "$(git -C source rev-parse HEAD)" = "$revision"
          . /etc/os-release
          test "$ID" = ubuntu
          test "$(uname -m)" = x86_64
          echo "Grease source pin: $revision"
          echo "Ubuntu: $PRETTY_NAME"

      - name: Prepare Oils build environment
        working-directory: source
        shell: bash
        run: |
          sudo sysctl vm.mmap_rnd_bits=28
          soil/worker.sh JOB-dev-setup-debian

      - name: Run inherited Oils cpp-spec workload
        working-directory: source
        shell: bash
        run: |
          . build/dev-shell.sh
          soil/worker.sh JOB-cpp-spec
          soil/host-shim.sh did-all-succeed cpp-spec
